# Core (installed for M0-M4 text path)
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# UAHP stack — PyPI `uahp==0.5.4` wheel is broken (imports missing modules).
# We currently vendor the identity pattern from PJ's uahp-stack/core.py into
# src/identity/uahp_identity.py. Re-enable the pin once a fixed upstream lands.
# uahp>=0.5.4

# Numeric
numpy>=1.24.0

# LLM clients
anthropic>=0.39.0
groq>=0.11.0
ollama>=0.3.0

# Vector store + embeddings
faiss-cpu>=1.7.4     # use faiss-gpu on workstation
sentence-transformers>=3.0.0

# Async + IO
aiofiles>=23.2.0

# CLI utilities
rich>=13.7.0
click>=8.1.7
jinja2>=3.1.0        # eval dashboard templates

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ------------------------------------------------------------------------
# Audio analysis + ElevenLabs generation (M5-M6 installed 2026-04-17)
soundfile>=0.12.1
librosa>=0.10.1
pyloudnorm>=0.2.0
pydub>=0.25.1
elevenlabs>=2.40.0

# ------------------------------------------------------------------------
# NOT installed yet (pulled in by future milestones):
# ------------------------------------------------------------------------
# Audio I/O (M0 round-trip / M14 bridge):
#   sounddevice>=0.4.6
#   webrtcvad>=2.0.10
#   opuslib>=3.0.1
# ASR (M1):
#   faster-whisper>=1.0.0
# TTS + emotional prosody (M5-M7, GPU-only):
#   TTS>=0.22.0
# Cloud bridge (M14):
#   websockets>=12.0
#   runpod>=1.6.0
# Eval dashboard (M11):
#   matplotlib>=3.8.0
#   pandas>=2.1.0
#   fastapi>=0.109.0
#   uvicorn>=0.27.0
# Orchestration:
#   asyncio-throttle>=1.0.2
# Dev tooling:
#   black>=23.12.0
#   ruff>=0.1.11
#   mypy>=1.8.0
#   pytest-cov>=4.1.0
#   python-json-logger>=2.0.7

